---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, tsconfig.json, tailwind.config.js, postcss.config.js, index.html, src/main.tsx, src/App.tsx, src/index.css]
autonomous: true
---

<objective>
Initialize Vite + React + TypeScript + Tailwind project with proper configuration.

Purpose: Create the foundation for the FormCheck web app with modern tooling.
Output: Working dev server with React, TypeScript, and Tailwind CSS configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite React TypeScript project</name>
  <files>package.json, vite.config.ts, tsconfig.json, index.html, src/main.tsx, src/App.tsx</files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` to scaffold the project.
    If directory not empty, handle by accepting overwrite or creating in current dir.
    After scaffold, run `npm install` to install dependencies.
  </action>
  <verify>npm run dev starts without errors, http://localhost:5173 shows Vite + React page</verify>
  <done>Vite dev server runs, React app renders</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind CSS</name>
  <files>tailwind.config.js, postcss.config.js, src/index.css</files>
  <action>
    Install Tailwind: `npm install -D tailwindcss postcss autoprefixer`
    Initialize: `npx tailwindcss init -p`
    Configure tailwind.config.js content paths: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
    Replace src/index.css with Tailwind directives:
    ```
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```
    Update App.tsx to use a Tailwind class (e.g., `className="min-h-screen bg-gray-900"`) to verify it works.
  </action>
  <verify>npm run dev shows styled page with Tailwind classes applied (dark background)</verify>
  <done>Tailwind CSS compiles and styles render correctly</done>
</task>

<task type="auto">
  <name>Task 3: Clean up scaffold and create base structure</name>
  <files>src/App.tsx, src/App.css, src/assets/</files>
  <action>
    Remove default Vite content from App.tsx - replace with minimal placeholder:
    - Dark background (bg-gray-900)
    - Centered "FormCheck" title
    - Placeholder text for camera area
    Delete src/App.css (using Tailwind instead).
    Delete src/assets/react.svg if exists.
    Keep structure simple - no routing yet.
  </action>
  <verify>npm run dev shows clean dark page with "FormCheck" title</verify>
  <done>Clean slate ready for camera component</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` completes without errors
- [ ] Page shows dark background with Tailwind styling
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- Vite dev server runs
- Tailwind CSS works
- Clean project structure ready for camera implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
